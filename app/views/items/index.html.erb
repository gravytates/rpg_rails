<h3>Items across the Galaxy</h3>

<% @items.each do |item| %>
 <p><%= item.name %>
   <%= form_for @user_item do |f| %>
     <%= f.hidden_field :item_id, value: item.id %>
     <%= f.submit "Add to Inventory" %>
   <% end %>
</p>
<% end %>

<%= link_to "Character Page", user_path(current_user) %>
